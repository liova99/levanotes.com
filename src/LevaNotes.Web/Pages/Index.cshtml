@page
@model IndexModel
@{
    ViewData["Title"] = "Home page";
}

@{
    bool isFirst = true;
    foreach (var post in Model.Posts)
    {
        <vc:post-preview post="@post" is-first="@isFirst"/>
        isFirst = false;
    }
    
}
